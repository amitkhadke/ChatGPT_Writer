// export default defineBackground(() => {
//   console.log('Hello background!', { id: browser.runtime.id });
// });

export default defineBackground(() => {
  console.log('LinkedIn AI Reply Extension Background Active!', { id: browser.runtime.id });

  // Optional: Add any persistent background tasks or event listeners here
});
